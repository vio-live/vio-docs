# Components

The Vio SDK provides SwiftUI components for live sports engagement. Most come from `VioCastingUI` (sports-specific overlays) and `VioEngagementUI` (engagement cards and overlays).

## Engagement Overlays (`VioEngagementUI`)

These are full-screen takeover overlays triggered automatically by the WebSocket when polls and contests fire.

### VioEngagementPollOverlay

Full-screen poll overlay. Shown automatically when a poll event arrives via WebSocket.

```swift
import VioEngagementUI

VioEngagementPollOverlay(
    question: "Hvem scorer neste mål?",
    subtitle: nil,
    options: [
        VioEngagementPollOverlayOption(id: "1", text: "FC Barcelona"),
        VioEngagementPollOverlayOption(id: "2", text: "PSG"),
    ],
    duration: 30,
    isChatExpanded: false,
    onVote: { optionId in /* handle vote */ },
    onDismiss: { /* handle dismiss */ }
)
```

### VioEngagementContestOverlay

Full-screen contest entry overlay.

```swift
VioEngagementContestOverlay(
    title: "Kampens spiller",
    subtitle: "Stem på den beste spilleren",
    imageUrl: "https://...",
    sponsorLogoUrl: "https://...",
    onParticipate: { /* handle entry */ },
    onDismiss: { }
)
```

### VioEngagementProductOverlay

Product grid overlay shown at sponsor slot minutes.

```swift
VioEngagementProductOverlay(
    products: products,
    isVisible: showProducts,
    onDismiss: { showProducts = false },
    onAddToCart: { product in Task { await cartManager.addProduct(product) } }
)
```

---

## Live Casting Components (`VioCastingUI`)

For full sports casting experiences. Import `VioCastingUI`.

### VCastingActiveView

The main overlay view for a live casting session. Combines match header, timeline, polls, contests, chat, and products.

```swift
import ViaCastingUI
import VioCore

VCastingActiveView(
    match: match,
    sessionContext: VioSessionContext(
        broadcastContext: BroadcastContext(broadcastId: "real-madrid-vs-barcelona-2025-01-24"),
        userId: "user_123"
    )
)
.environmentObject(cartManager)
```

### MatchHeaderView

Live match score, team logos, and match status.

```swift
MatchHeaderView(match: match)
```

Score updates automatically via polling to `/v1/sdk/broadcasts/:id/score` every 30 seconds.

### TimelinePollCard

Poll card displayed in the match timeline feed.

```swift
TimelinePollCard(
    poll: poll,
    sponsorAssets: sponsorAssets,
    onVote: { optionId in /* handle */ }
)
```

### ContestCard

Contest participation card in the timeline feed.

```swift
ContestCard(
    contest: contest,
    sponsorAssets: sponsorAssets,
    onParticipate: { /* handle */ }
)
```

### SponsorBanner

Sponsor logo and branding bar. Assets come from backend automatically.

```swift
SponsorBanner()
```

### VCastingProductCardWrapper / VCastingProductModal

Shoppable product card and its detail modal.

```swift
VCastingProductCardWrapper(
    product: product,
    onTap: { showModal = true }
)

VCastingProductModal(
    product: product,
    isPresented: $showModal,
    onAddToCart: { Task { await cartManager.addProduct(product) } }
)
```

### AllContentFeed

Main content feed showing all timeline events: polls, contests, announcements, stats.

```swift
AllContentFeed(match: match)
```

### MatchStatsView / MatchLineupView

Match statistics and lineup components.

```swift
MatchStatsView(match: match)
MatchLineupView(match: match)
```

### TweetCard / AnnouncementCard / CommentaryCard

Social and live commentary cards for the timeline feed.

```swift
TweetCard(tweet: tweet, sponsorAssets: sponsorAssets)
AnnouncementCard(announcement: announcement)
CommentaryCard(commentary: commentary)
```

---

## Product Components (`VioUI`)

For shoppable product displays. Import `VioUI`.

### ProductService

Load products from the Commerce API.

```swift
import VioUI

let products = try await ProductService.shared.loadProducts(
    productIds: [408841, 408895],
    currency: "NOK",
    country: "NO"
)
```

### CartManager + VioFloatingCartIndicator + VioCheckoutOverlay

```swift
@StateObject private var cartManager = CartManager()

// In your view
.overlay { VioFloatingCartIndicator().environmentObject(cartManager) }
.sheet(isPresented: $cartManager.isCheckoutPresented) {
    VioCheckoutOverlay()
        .environmentObject(cartManager)
        .environmentObject(checkoutDraft)
}
```

---

## Design System (`VioDesignSystem`)

```swift
import VioDesignSystem

Text("LIVE")
    .foregroundColor(VioColors.primary.toColor())
    .font(.system(size: VioTypography.caption))

VStack(spacing: VioSpacing.md) {
    VioButton(title: "Stem nå", style: .primary) { }
}
```

---

## Next Steps

- [Core Concepts](/swift/core-concepts) — How CampaignManager and engagement flow together
- [API Reference](/swift/api-reference) — Models, services, manager APIs
- [Complete Setup Example](/swift/examples/complete-setup) — Full integration
